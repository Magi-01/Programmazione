#include <stdio.h>
//#include <math.h>
#define N 3

int somma (int v[],int n)
{
  int t = 0;
  for (int i=0; i<n; i++)
    {
      t += a[i];
    }
  return t;
}

int main()
{
  int a[N];
  printf ("n: ");
  scanf("%d",n)
  
  for (int i=0;i<N;i++)
    {
      a[i] = i;
    }

  m  = somma (a[],n);

  printf("La somma è: %d\n",m);
  
  
  
  return 0;
}