#include <stdio.h>

int main()
{
  int a,b,x=1;

  printf("insert first number x: ");
  scanf("%d",&x); 
  printf("insert second number Natural y: ");

  for(int i=0;i<b;i++)
    {
      x=x*a;
    }
  printf("the power of %d to the %d gives: %d\n",a,b,x);
}